import { c, cB, cM } from '../../../_utils/cssr'

export default c([
  cB('auto-complete', [
    cM('rtl', 'direction: rtl;')
  ]),
  cB('auto-complete-menu', [
    cM('rtl', 'direction: rtl;')
  ])
])
